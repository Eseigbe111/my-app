/* this is how ternary works :arrow_down:below
condition ? expressionIfTrue : expressionIfFalse; */

let age = 18

let Allowed = (age >= 18) ? 'yes': 'false'

//console.log(Allowed)

let x = 6

let y = (x >= 5) ? 'x is greater than 5': 'x is less than or equal to 5'

//console.log(y)


let num = 20

let number = (num % 2 === 0) ? `${num} is an even number.` : `${num} is not even.`

console.log(number)